{
  "name": "event-management-app",
  "version": "1.0.0",
  "description": "Event Management Application for organizing weddings, corporate events, birthdays, and cultural programs",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["event-management", "wedding", "corporate-events"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

